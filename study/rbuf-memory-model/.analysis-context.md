# rbuf 内存模型 - 分析上下文

## 项目概况

- **项目名称**：Cyclone DDS - rbuf 接收缓冲区内存模型
- **类型**：源码项目（C 语言中间件）
- **源码根路径**：`src/cyclonedds/src/core/ddsi/`
- **LSP 状态**：已使用（clangd，基于 compile_commands.json）
- **study 目录**：`study/rbuf-memory-model/`

## 源码路径映射

| 模块 | 文件路径 |
|:--|:--|
| 公共 API | `src/core/ddsi/include/dds/ddsi/ddsi_radmin.h` |
| 内部头文件 | `src/core/ddsi/src/ddsi__radmin.h` |
| 主实现 | `src/core/ddsi/src/ddsi_radmin.c` |
| 接收路径（消费者） | `src/core/ddsi/src/ddsi_receive.c` |
| 域全局 | `src/core/ddsi/include/dds/ddsi/ddsi_domaingv.h` |
| 配置 | `src/core/ddsi/include/dds/ddsi/ddsi_config.h` |
| 测试 | `src/core/ddsi/tests/radmin.c` |

**从 study 目录到源码根的相对路径**：`../../src/cyclonedds/`

## 学习大纲

| 章节 | 文件名 | 标题 | 对应模块 | 内容要点 |
|:--|:--|:--|:--|:--|
| 0 | 00-overview.md | 快速导览 | 全部 | 四层内存体系、三级处理管线、典型收包流程、架构全景图、学习路线 |
| 1 | 01-rbufpool-rbuf.md | 缓冲池与接收缓冲区 | rbufpool + rbuf | 池化管理、线程所有权模型、顺序分配策略、rbuf 生命周期、引用计数 |
| 2 | 02-rmsg-rdata.md | 消息管理与引用计数 | rmsg + rdata | rmsg_chunk 链式扩展、双偏置引用计数、commit 语义、rdata 零拷贝偏移量 |
| 3 | 03-defrag.md | 分片重组机制 | defrag | AVL 树索引、区间合并算法、rsample 双态 union、内存复用、丢弃策略 |
| 4 | 04-reorder.md | 序列重排机制 | reorder | 序列号区间树、三种模式、gap 处理、refcount 调整协议、nackmap 生成 |
| 5 | 05-dqueue.md | 投递队列 | dqueue | bubble 机制、背压控制、deaf 模式、线程模型、回调调度 |

## 模块分析摘要

### 模块 1：rbufpool + rbuf（ddsi_radmin.c:264-518）

**核心函数**：

| 函数名 | 位置 | 职责 |
|:--|:--|:--|
| `ddsi_rbufpool_new` | ddsi_radmin.c:333 | 创建缓冲池，分配首个 rbuf |
| `ddsi_rbufpool_setowner` | ddsi_radmin.c:377 | 设置池的所有者线程（debug 用） |
| `ddsi_rbufpool_free` | ddsi_radmin.c:384 | 释放缓冲池及当前 rbuf |
| `ddsi_rbuf_alloc_new` | ddsi_radmin.c:427 | 分配新 rbuf（malloc + 柔性数组） |
| `ddsi_rbuf_new` | ddsi_radmin.c:448 | 替换当前 rbuf 为新分配的 |
| `ddsi_rbuf_release` | ddsi_radmin.c:463 | 原子减引用，归零时 free |
| `ddsi_rbuf_alloc` | ddsi_radmin.c:491 | 从当前 rbuf 顺序分配空间 |

**关键数据结构**：`ddsi_rbufpool`（L264-288）、`ddsi_rbuf`（L403-425）

**设计特点**：单线程分配 + 多线程释放，顺序分配不回收（freeptr 只增不减）

### 模块 2：rmsg + rdata（ddsi_radmin.c:474-764）

**核心函数**：

| 函数名 | 位置 | 职责 |
|:--|:--|:--|
| `ddsi_rmsg_new` | ddsi_radmin.c:528 | 在 rbuf 上分配 rmsg，设初始引用计数 |
| `ddsi_rmsg_setsize` | ddsi_radmin.c:550 | 设置实际接收数据大小 |
| `ddsi_rmsg_commit` | ddsi_radmin.c:603 | 提交消息，无引用则立即释放 |
| `ddsi_rmsg_free` | ddsi_radmin.c:566 | 遍历 chunk 链释放所有 rbuf 引用 |
| `ddsi_rmsg_alloc` | ddsi_radmin.c:672 | 在 rmsg 后追加分配（chunk 溢出则新建） |
| `ddsi_rmsg_addbias` | ddsi_radmin.c:635 | 加 RDATA_BIAS（$2^{20}$）偏置 |
| `ddsi_rmsg_rmbias_and_adjust` | ddsi_radmin.c:649 | 移除偏置并调整实际引用数 |
| `ddsi_rdata_new` | ddsi_radmin.c:716 | 在 rmsg 中分配 rdata，记录偏移量 |

**关键常量**：`RMSG_REFCOUNT_UNCOMMITTED_BIAS` = $2^{31}$，`RMSG_REFCOUNT_RDATA_BIAS` = $2^{20}$

**关键数据结构**：`ddsi_rmsg`（公共头 L53-93）、`ddsi_rmsg_chunk`（L34-51）、`ddsi_rdata`（L98-108）

### 模块 3：defrag（ddsi_radmin.c:766-1562）

**核心函数**：

| 函数名 | 位置 | 职责 |
|:--|:--|:--|
| `ddsi_defrag_new` | ddsi_radmin.c:885 | 创建 defrag 实例 |
| `ddsi_defrag_rsample` | ddsi_radmin.c:1324 | 接收 rdata，完整返回 rsample，不完整存储 |
| `defrag_add_fragment` | ddsi_radmin.c:1161 | 区间树插入/合并核心算法 |
| `defrag_try_merge_with_succ` | ddsi_radmin.c:960 | 尝试与后续区间合并 |
| `is_complete` | ddsi_radmin.c:1110 | 判断是否所有分片已齐 |
| `rsample_convert_defrag_to_reorder` | ddsi_radmin.c:1135 | defrag union 切换为 reorder |
| `defrag_limit_samples` | ddsi_radmin.c:1282 | 淘汰策略（oldest/latest） |
| `ddsi_defrag_nackmap` | ddsi_radmin.c:1449 | 生成 NACK 位图 |

**关键数据结构**：`ddsi_defrag`（L854-863）、`ddsi_defrag_iv`（L829-834）、`ddsi_rsample`（L836-852，union 双态）

### 模块 4：reorder（ddsi_radmin.c:1553-2487）

**核心函数**：

| 函数名 | 位置 | 职责 |
|:--|:--|:--|
| `ddsi_reorder_new` | ddsi_radmin.c:1669 | 创建 reorder 实例 |
| `ddsi_reorder_rsample` | ddsi_radmin.c:1895 | 将 sample 插入重排树，可能触发投递 |
| `ddsi_reorder_gap` | ddsi_radmin.c:2224 | 处理 Gap 消息 |
| `reorder_try_append_and_discard` | ddsi_radmin.c:1755 | 尝试合并相邻区间 |
| `ddsi_reorder_rsample_dup_first` | ddsi_radmin.c:1790 | 为 out-of-sync reader 复制 sample |
| `ddsi_reorder_nackmap` | ddsi_radmin.c:2379 | 生成 NACK 序列号位图 |
| `ddsi_reorder_drop_upto` | ddsi_radmin.c:2336 | 丢弃指定序列号之前的所有 sample |

**关键数据结构**：`ddsi_reorder`（L1653-1664）

**三种模式**：NORMAL（严格排序）、MONOTONICALLY_INCREASING（单调递增）、ALWAYS_DELIVER（立即投递）

### 模块 5：dqueue（ddsi_radmin.c:2494-2900）

**核心函数**：

| 函数名 | 位置 | 职责 |
|:--|:--|:--|
| `ddsi_dqueue_new` | ddsi_radmin.c:2690 | 创建投递队列 |
| `ddsi_dqueue_start` | ddsi_radmin.c:2716 | 启动队列处理线程 |
| `dqueue_thread` | ddsi_radmin.c:2597 | 队列消费线程主循环 |
| `ddsi_dqueue_enqueue` | ddsi_radmin.c:2766 | 入队（唤醒消费线程） |
| `ddsi_dqueue_enqueue_deferred_wakeup` | ddsi_radmin.c:2746 | 入队（延迟唤醒，批量优化） |
| `ddsi_dqueue_enqueue1` | ddsi_radmin.c:2807 | 带 reader GUID 入队（多 reader 路由） |
| `ddsi_dqueue_enqueue_callback` | ddsi_radmin.c:2797 | 入队回调 bubble |
| `ddsi_dqueue_is_full` | ddsi_radmin.c:2828 | 检查背压 |
| `ddsi_dqueue_step_deaf` | ddsi_radmin.c:2550 | deaf 模式下丢弃所有排队数据 |

**关键数据结构**：`ddsi_dqueue`（L2494-2506）、`ddsi_dqueue_bubble`（L2521-2537）

**三种元素类型**：DATA（数据）、GAP（间隙）、BUBBLE（控制信号：STOP/CALLBACK/RDGUID）

## 格式规范摘要

完整规范见 skill 目录下的 format-rules.md，关键规则：
- 源码位置格式模板：> 📍 源码：[文件名:起始行-结束行](相对路径#L起始行)
- 相对路径前缀：../../src/cyclonedds/src/core/ddsi/
- Mermaid 图表：节点 $\leq$ 20，同层 $\leq$ 6，标题 $\leq$ 10 中文字
- 链接文本不加反引号；无链接标识符必须加反引号
- 数学符号用 LaTeX（$2^{31}$），禁止 Unicode 数学符号
- 代码块必须指定语言标识
